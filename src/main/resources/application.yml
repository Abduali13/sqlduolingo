
spring:
  application:
    name: sqlduolingo
  datasource:
    url: jdbc:postgresql://localhost:5432/sqlduolingo
    username: sqlduolingo_user
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#  main:
#    allow-circular-references: true

server:
  port: 8080
